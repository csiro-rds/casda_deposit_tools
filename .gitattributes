# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# All *.sh files should be checked out with line-feeds (prevents issues in cygwin)
*.sh eol=lf

# Denote all files that are truly binary and should not be modified.
*.fits binary diff=fits
*.tar binary
*.pdf binary

